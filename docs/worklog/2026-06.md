# Worklog - 2026-06

## 2026-06-01

- Completed Alexa Custom Skill MVP for Maggiordomo Albert.
- Created and validated custom Alexa skill invocation using a private invocation name.
- Configured Alexa HTTPS endpoint through Cloudflare Tunnel to the FastAPI bridge.
- Validated Cloudflare Tunnel forwarding to FastAPI/Uvicorn on port 5055.
- Added Alexa-compatible FastAPI endpoint:
  - `POST /alexa`
  - `POST /alexa/laundry`
- Added bridge health/status routes:
  - `GET /`
  - `GET /health`
  - `GET /laundry/status`
- Validated Alexa LaunchRequest end-to-end.
- Fixed Alexa session handling by keeping the session open after launch.
- Validated laundry status query through Alexa Custom Skill.
- Identified Alexa Developer Console endpoint issue:
  - Cloudflare certificate was wildcard.
  - Alexa endpoint SSL type must be set to wildcard certificate.
- Identified interaction model issue:
  - Template `HelloWorldIntent` intercepted the laundry utterance.
  - Temporary bridge mapping routes `HelloWorldIntent` to laundry status until interaction model cleanup.
- Security note:
  - Alexa debug payloads contain user/device/token identifiers and must never be committed.
  - Real endpoint must be sanitized in public repository docs.

## 2026-06-02 - Alexa Custom Skill Laundry Intent

- Renamed assistant persona from Albert to Alfred.
- Validated Alexa Custom Skill launch flow with Cloudflare HTTPS bridge.
- Confirmed end-to-end path:
  - Alexa Custom Skill
  - Cloudflare Tunnel
  - FastAPI bridge
  - Home Assistant
  - hOn washing machine status
- Refactored the local FastAPI bridge into smaller modules:
  - config
  - Home Assistant client
  - Alexa response helpers
  - laundry service
  - Alfred response helper
- Added Alfred SSML response helper and tested Polly voice options.
- Selected a temporary supported voice strategy after testing voice compatibility.
- Added Supported Features response for Alfred.
- Created and validated LaundryStatusIntent.
- Confirmed LaundryStatusIntent reaches the backend and returns washing machine status.
- Result:
  - "Ask Alfred The Butler how much time is left on the washing machine"
  - returns the current washing machine connectivity/status response.
- Next:
  - clean up sample HelloWorld intent
  - add laundry program catalog intent
  - update sanitized bridge examples
  - update public project model snapshot

## 2026-06-06 - Alfred Laundry Start/Stop and Catalog UX

- Extended Alfred the Butler laundry workflow beyond read-only status.
- Recovered and integrated the real Home Assistant laundry program catalog:
  - 143 validated washing machine programs
  - internal program codes
  - Italian names
  - categories
  - default parameters
- Added catalog-driven laundry start logic:
  - voice name is mapped to a validated catalog entry
  - internal hOn program code is used for execution
  - program defaults are read from the catalog
  - unknown programs are rejected safely
- Added deterministic program-name normalization:
  - supports spoken variants such as "con vapore" and "più vapore"
  - avoids fuzzy matching for appliance control
- Added Home Assistant script wrapper for physical hOn program start:
  - Python/FastAPI owns validation, catalog and voice logic
  - Home Assistant owns the device action wrapper
- Validated remote start/stop MVP:
  - start command dispatch works through Alfred backend
  - stop command dispatch works through Alfred backend
  - status intent remains the source of truth after command dispatch
- Changed Alexa start/stop UX to quick responses:
  - commands are acknowledged quickly
  - Alfred no longer waits for long hOn polling during Alexa sessions
  - users are prompted to ask for laundry status after a few seconds
- Added/updated laundry intents:
  - LaundryStatusIntent
  - LaundryLastIntent
  - LaundryStartIntent
  - LaundryStopIntent
  - LaundryAvailableProgramsIntent
  - LaundryProgramsByCategoryIntent
  - LaundrySearchProgramsIntent
  - AlfredExitIntent
- Improved Alfred session behavior:
  - functional responses keep the Alexa session open
  - explicit exit/thanks phrases close the session
  - launch flow uses an Alexa-to-Alfred handoff response
- Added category-based laundry catalog queries:
  - bianchi
  - colorati
  - scuri
  - delicati
  - igiene
  - manutenzione
  - animali
  - rapidi
  - sport
  - macchie
  - vapore
  - sintetici
- Added compatibility handling for Alexa routing catalog phrases to search intent.
- Documented observed hOn behavior:
  - sensor updates can lag after start/stop
  - stop may physically occur before Home Assistant sensors refresh
  - repeated control tests can temporarily make washer entities unavailable
- Updated private project model.
- Generated public project model snapshot for 2026-06-06.

- Next:
  - clean up Alexa Developer Console model
  - implement true keyword search across program names and categories
  - add async start/stop verification notifications
  - update sanitized examples and case study documentation

## 2026-06-07 - Alfred laundry proactive prompt MVP

- Validated Alfred laundry start/stop flow through Alexa Custom Skill, FastAPI bridge, Home Assistant and hOn.
- Confirmed `LaundryStopIntent` routes correctly and is separate from `AMAZON.StopIntent`, which remains skill/session exit only.
- Added short-lived laundry assist pending context in the FastAPI bridge.
- Added Home Assistant `rest_command.alfred_laundry_assist_pending`.
- Added washing-machine-connected automation that prepares Alfred context when the washer comes online with remote control active.
- Added Alexa/Echo prompt: "Ho notato che hai acceso la lavatrice. Se vuoi una mano con il bucato, dì: Alexa, apri Alfred the Butler."
- External proactive prompt uses normal Alexa notification voice as plain text, not Alfred/Giorgio SSML.
- Alfred/Giorgio voice remains reserved for Custom Skill responses.
- Added `LaundryAssistYesIntent` so "sì grazie" continues the Alfred laundry assistance flow instead of closing the session.
- Validated manual automation trigger: Home Assistant calls `/laundry/assist-pending` and the bridge returns 200 OK.
- Release notification target is the living-room Echo notify entity; the near-field Echo test target remains debug-only.
- Known follow-ups: add cooldown/time-window guards, async start/stop verification, true keyword search, cleanup duplicate `laundry_last_completed_cycle` automation id.
- Public docs must sanitize raw Alexa payloads, skill IDs, webhook URLs, endpoints, private IPs and device identifiers.

## 2026-06-07 - Alfred laundry prompt guardrails and dynamic menu

- Added a 30-minute cooldown for the washing-machine-connected proactive prompt using a Home Assistant input datetime helper.
- Validated cooldown behavior: a recent prompt timestamp blocks `/laundry/assist-pending`.
- Added a daytime window for the proactive laundry prompt.
- Added a no-active-program guard: prompt is allowed only when remaining time is `0` and program phase is idle.
- Validated idle logic with current hOn sensors: remaining time `0`, phase `0`, prompt allowed.
- Updated Alfred's laundry assistance menu to be state-aware.
- When the washer appears idle, Alfred offers status, program discovery, category search and validated start commands.
- When the washer appears active, Alfred should only offer status / remaining time and stop.
- Kept explicit stop command available as a cautious best-effort command even if sensors may be stale.
- Follow-up: validate active-cycle menu during a real running wash.

## 2026-06-07 - Alfred laundry program name fallback

- Investigated hOn washing machine remote-start program naming.
- Confirmed remote start sends and accepts the selected program code, but hOn may later expose generic names such as `HOME_ASSISTANT`, `home_assistant` or `No program`.
- Opened an upstream community integration issue with sanitized findings.
- Added a Home Assistant input text helper to store the human-readable program name requested by Alfred.
- Updated the HA laundry start script to save the requested display name before calling hOn start.
- Updated the FastAPI bridge to pass the validated Italian program name to the HA script.
- Normalized hOn default start parameters to strings to avoid enum comparison warnings for values such as temperature and spin speed.
- Updated Alfred laundry status fallback: when hOn exposes a generic or unusable program name, Alfred uses the stored requested program name.
- Validated final status response: Alfred reports the requested program name and remaining time correctly.
- Public docs must not include raw hOn payloads, MAC addresses, mobile IDs, device IDs, transaction IDs, skill IDs or private entity names.

## 2026-06-07 - Alfred laundry voice UX and async verification

- Improved Alfred Custom Skill session UX with reprompts for open conversations.
- Updated launch and fallback responses to suggest asking "che cosa puoi fare" when the user needs help.
- Validated help routing for supported features.
- Implemented true keyword search across the laundry program catalog instead of category-only search.
- Added paginated voice results for long program lists:
  - first 5 results are read immediately
  - "sì" continues with the next page
  - "no" stops the list without closing the skill
- Added defensive routing so exit-like intents during a pending search list are treated as stopping the list.
- Added a Home Assistant helper to preserve the human-readable program name requested by Alfred.
- Updated laundry status fallback so generic hOn program names such as `home_assistant` or `No program` are replaced with Alfred's stored requested program name.
- Normalized hOn default program parameters as strings to avoid enum comparison issues for values such as temperature and spin speed.
- Investigated hOn remote-start naming behavior and opened an upstream community integration issue with sanitized findings.
- Added asynchronous start/stop verification:
  - Alfred responds immediately after dispatching the command
  - verification runs out of band
  - hOn state is refreshed before each verification attempt
  - verification polls every 15 seconds up to 90 seconds
  - success is announced only when the expected state is observed
  - if confirmation is not reached in time, Alfred asks for manual verification
- Validated async start verification:
  - first attempt not confirmed
  - second attempt confirmed the washer as running
  - notification included the requested program name and estimated remaining time
- Validated async stop verification:
  - several attempts not confirmed while hOn lagged
  - later attempt confirmed the washer as stopped
  - notification reported the washer as stopped
- Public docs must not include raw Alexa payloads, skill IDs, user IDs, slot resolution IDs, hOn payloads, MAC addresses, mobile IDs, device IDs, transaction IDs, real endpoints, private IPs or secrets.

### Checkout follow-ups

- Update sanitized Alexa Custom Skill case study with:
  - reprompt UX
  - keyword search pagination
  - async verification with retry window
  - hOn delayed sensor behavior
- Add sanitized examples for:
  - program-name helper
  - async verification notifications
  - search pagination intent flow
  - Home Assistant laundry start script fields
- Regenerate private project model and public snapshot.

## 2026-06-07 - Alfred laundry voice UX and async verification

- Improved Alfred Custom Skill session UX with reprompts for open conversations.
- Updated launch and fallback responses to suggest asking "che cosa puoi fare" when the user needs help.
- Validated help routing for supported features.
- Implemented true keyword search across the laundry program catalog instead of category-only search.
- Added paginated voice results for long program lists:
  - first 5 results are read immediately
  - "sì" continues with the next page
  - "no" stops the list without closing the skill
- Added defensive routing so exit-like intents during a pending search list are treated as stopping the list.
- Added a Home Assistant helper to preserve the human-readable program name requested by Alfred.
- Updated laundry status fallback so generic hOn program names such as `home_assistant` or `No program` are replaced with Alfred's stored requested program name.
- Normalized hOn default program parameters as strings to avoid enum comparison issues for values such as temperature and spin speed.
- Investigated hOn remote-start naming behavior and opened an upstream community integration issue with sanitized findings.
- Added asynchronous start/stop verification:
  - Alfred responds immediately after dispatching the command
  - verification runs out of band
  - hOn state is refreshed before each verification attempt
  - verification polls every 15 seconds up to 90 seconds
  - success is announced only when the expected state is observed
  - if confirmation is not reached in time, Alfred asks for manual verification
- Validated async start verification:
  - first attempt not confirmed
  - second attempt confirmed the washer as running
  - notification included the requested program name and estimated remaining time
- Validated async stop verification:
  - several attempts not confirmed while hOn lagged
  - later attempt confirmed the washer as stopped
  - notification reported the washer as stopped
- Public docs must not include raw Alexa payloads, skill IDs, user IDs, slot resolution IDs, hOn payloads, MAC addresses, mobile IDs, device IDs, transaction IDs, real endpoints, private IPs or secrets.

### Checkout follow-ups

- Update sanitized Alexa Custom Skill case study with:
  - reprompt UX
  - keyword search pagination
  - async verification with retry window
  - hOn delayed sensor behavior
- Add sanitized examples for:
  - program-name helper
  - async verification notifications
  - search pagination intent flow
  - Home Assistant laundry start script fields
- Regenerate private project model and public snapshot.
